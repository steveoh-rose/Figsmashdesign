
  # FigSmash

  This is a code bundle for FigSmash. The original project is available at https://www.figma.com/design/5TCPf5iEnGS6HfH8PHjOyY/FigSmash.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  